.envrc
.direnv/
node_modules/
dist/
.vite/
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
.env.*.local

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# Personal execution checklist (HTML dashboard, Daniel's local pane)
execution-checklist.html

# Tester-cohort invite roster (raw tokens + per-tester share links)
tester-invites.md

# Project vault — moved to private companion repo of3y/vyou-vault
docs/
CLAUDE.md

# Claude Code session-local config and skills (private)
.claude/
.vercel

# Hackathon submission artifacts (kept locally, removed from public tree)
demo/
remotion-player/

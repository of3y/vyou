.envrc
.direnv/
node_modules/
dist/
.vite/
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
.env.*.local

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# Personal execution checklist (HTML dashboard, Daniel's local pane)
execution-checklist.html

# Tester-cohort invite roster (raw tokens + per-tester share links)
tester-invites.md

# Obsidian personal pane/cursor state — constantly churns, not shared
docs/.obsidian/workspace.json
docs/.obsidian/workspace-mobile.json

# Claude Code scheduled-task lock (local runtime state)
.claude/scheduled_tasks.lock
.vercel

# Demo media + Remotion build outputs (large, regenerable)
demo/assets/
demo/out/
demo/renders/

# Remotion player build artifacts
remotion-player/node_modules/
remotion-player/dist/
remotion-player/*.tsbuildinfo
